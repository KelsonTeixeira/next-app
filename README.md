## About it
This project is result of a NextJS Course.

## Getting Started

First, run the development server:

```bash
npm i
npm run dev
npm run server
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

The server (db) is running on [http://localhost:3001](http://localhost:3001)

